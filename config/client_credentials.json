{
  "_comment": "Dashboard client login credentials. The public dashboard view is visible to anyone who opens the page; logging in unlocks the client view + edit/calc config buttons. Store SHA-256 hashes (hex, 64 chars) of the username and password SEPARATELY. Use the hash_creds.py helper in this repo to generate them. Leave both fields empty to disable login entirely (public view only).",
  "username_hash": "",
  "password_hash": ""
}